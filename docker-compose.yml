version: '3.8'

services:
  web:
    build: .
    container_name: django_sales_app
    ports:
      - "8000:8000"
    environment:
      - DJANGO_SETTINGS_MODULE=config.settings
      - DEBUG=1
      - ALLOWED_HOSTS=*
